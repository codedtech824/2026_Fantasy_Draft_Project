# Data files are intentionally versioned (not ignored) -- a fresh clone
# ships with a working draft board so the pipeline can be demoed without
# first running a full live fetch. Regenerate via 01-05 / run_pipeline.py.

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Logs
pipeline.log
*.log

# OS
.DS_Store
Thumbs.db
